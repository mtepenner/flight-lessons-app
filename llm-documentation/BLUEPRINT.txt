/flight-lessons-app
├── .env.example                              # Provides clear environment variable templates for local setup.
├── .gitignore                                # Ensure all keys are not uploaded to the repository
├── netlify.toml                              # Configures Netlify to build the React app and points to the serverless functions folder.
├── package.json
├── README.md                                 # Documents your architecture, tradeoffs, and explains the double-booking prevention logic.
├── vite.config.js                            # Bootstraps the React app using Vite for fast building.
├── netlify/
│   └── functions/
│       └── get-confirmation.js               # Secure server-side middleman where the Claude API key lives to generate confirmations.
├── supabase/
│   └── schema.sql                            # Stores the UNIQUE constraints for double-booking and Row Level Security (RLS) policies.
└── src/
    ├── App.jsx
    ├── index.css
    ├── main.jsx
    ├── supabaseClient.js                     # Initializes Supabase using the URL and public Anon key (safe for the browser).
    └── components/
        ├── Auth.jsx                          # Handles customer authentication (Supabase Auth) so users can log in.
        ├── Dashboard.jsx                     # Displays the user's specific bookings and the AI-generated success message.
        ├── FlightBlocks.jsx                  # Shows the available pre-packaged time slots for flight lessons.
        └── Toast.jsx                         # UI component that catches and displays the Postgres constraint violation if two users book simultaneously.
